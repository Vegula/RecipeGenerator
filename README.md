# Recipe Manager System

A Java-based application to manage and store cooking recipes.

## 🚀 Features
* Add, view, and manage recipes.
* Persistent storage: Recipes are saved to a local database file.

## 📂 File Structure
* `RecipeManager.java`: The core logic for handling recipe operations.
* `recipe_database...`: The storage file where recipe data is saved.

## 🛠️ How to Run
1. Ensure you have Java (JDK) installed.
2. Compile the program:  
   `javac RecipeManager.java`
3. Run the program:  
   `java RecipeManager`

## 📝 Note
The program automatically creates/updates the `recipe_database` file in the root directory.